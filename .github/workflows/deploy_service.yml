name: Deploy to Cloud Run

on:
  push:
    branches: [ main ]

env:
  PROJECT_ID: phonic-altar-450817-q4
  SERVICE_NAME: events-service
  REGION: europe-southwest1
  IMAGE_NAME: gcr.io/phonic-altar-450817-q4/events-service

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up JDK 11
      uses: actions/setup-java@v4
      with:
        java-version: '11'
        distribution: 'temurin'

    - name: Authenticate to Google Cloud
      uses: google-github-actions/auth@v2
      with:
        credentials_json: ${{ secrets.GCP_SA_KEY }}

    - name: Set up Cloud SDK
      uses: google-github-actions/setup-gcloud@v2

    - name: Configure Docker for GCR
      run: gcloud auth configure-docker

    - name: Build and package
      # working-directory: ./events-service
      run: ./mvn clean package

    - name: Build Docker image
      working-directory: ./events-service
      run: |
        docker build \
          -f src/main/docker/Dockerfile.jvm \
          --platform=linux/amd64 \
          -t ${{ env.IMAGE_NAME }}:${{ github.sha }} .

    - name: Push to Container Registry
      run: docker push ${{ env.IMAGE_NAME }}:${{ github.sha }}

    - name: Deploy to Cloud Run
      run: |
        gcloud run deploy ${{ env.SERVICE_NAME }} \
          --image=${{ env.IMAGE_NAME }}:${{ github.sha }} \
          --platform=managed \
          --region=${{ env.REGION }} \
          --allow-unauthenticated \
          --port=8080 \
          --memory=1Gi \
          --cpu=1 \
          --set-env-vars="GOOGLE_APPLICATION_CREDENTIALS=/app/firebase-key.json" \
          --update-secrets="/app/firebase-key.json=firebase-credentials:latest" \
          --project=${{ env.PROJECT_ID }}
